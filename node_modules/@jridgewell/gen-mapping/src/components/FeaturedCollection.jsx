import React from 'react'
import ProductCard from './ProductCard'
export default function FeaturedCollection({products=[]}){
  return (
    <section className="container mx-auto px-4 py-8 bg-gradient-to-b from-white to-amber-50 rounded-lg">
      <div className="flex items-center justify-between mb-6">
        <h3 className="text-2xl font-semibold text-slate-900">Featured collection</h3>
        <a href="/collections" className="text-sm text-amber-600 font-medium">View all →</a>
      </div>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {products.map(p=> <ProductCard key={p.id} product={p} onAdd={()=>alert(`${p.title} added`)} />)}
      </div>
    </section>
  )
}
