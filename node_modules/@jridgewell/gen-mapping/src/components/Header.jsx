import React, { useState } from 'react'
import { Link, NavLink } from 'react-router-dom'
import { useCart } from '../context/CartContext'

export default function Header() {
  const { count } = useCart()
  const [mobileOpen, setMobileOpen] = useState(false)

  return (
    <header className="sticky top-0 z-50">
      <div className="bg-white/90 backdrop-blur-sm border-b border-gray-200">
        <div className="container mx-auto px-4 py-3 flex items-center justify-between gap-4">
          {/* Logo */}
          <Link to='/' className="flex items-center gap-3">
            <div className="w-12 h-12 rounded-full bg-gradient-to-br from-gray-900 to-gray-700 flex items-center justify-center text-white text-xl font-bold shadow-md">VV</div>
            <div className="hidden sm:block">
              <div className="text-lg font-semibold">Visvas Verve</div>
              <div className="text-sm text-muted">Sustainable · Premium</div>
            </div>
          </Link>

          {/* Search - show on md+ */}
          <div className="flex-1 hidden md:flex items-center justify-center">
            <form className="w-full max-w-2xl flex items-center gap-2 bg-white rounded-full shadow-sm border border-gray-200 px-2 py-2">
              <select className="bg-transparent text-sm text-gray-600 px-3 py-2 rounded-l-full border-r border-gray-100">
                <option>All</option>
                <option>Clothing</option>
                <option>Carrybags</option>
              </select>
              <input className="flex-1 outline-none px-3 py-2 text-sm" placeholder="Search products, e.g. organic tee" />
              <button className="btn-premium rounded-full px-4 py-2">Search</button>
            </form>
          </div>

          {/* Actions */}
          <div className="flex items-center gap-3">
            <nav className="hidden lg:flex items-center gap-4 text-sm">
              <NavLink to="/collections/formal" className={({isActive})=>isActive? 'text-gray-900 font-medium':'text-muted'}>Formal</NavLink>
              <NavLink to="/collections/casual" className={({isActive})=>isActive? 'text-gray-900 font-medium':'text-muted'}>Casual</NavLink>
              <NavLink to="/sustainability" className={({isActive})=>isActive? 'text-gray-900 font-medium':'text-muted'}>Sustainability</NavLink>
            </nav>

            <Link to="/login" className="hidden sm:inline text-sm text-gray-700">Sign in</Link>

            <Link to="/cart" className="relative inline-flex items-center gap-2 px-3 py-2 rounded-md hover:bg-gray-50 text-gray-700">
              <svg xmlns="http://www.w3.org/2000/svg" className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.6" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2 6m13-6l2 6M10 21a1 1 0 100-2 1 1 0 000 2zm6 0a1 1 0 100-2 1 1 0 000 2z" />
              </svg>
              {count > 0 && <span className="absolute -top-1 -right-1 inline-flex items-center justify-center text-xs font-semibold bg-red-600 text-white rounded-full w-5 h-5">{count}</span>}
            </Link>

            <button className="md:hidden p-2" onClick={()=>setMobileOpen(!mobileOpen)} aria-label="menu">
              <svg xmlns="http://www.w3.org/2000/svg" className="w-6 h-6 text-gray-700" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>

      {/* Mobile panel */}
      {mobileOpen && (
        <div className="md:hidden bg-white border-b border-gray-100">
          <div className="px-4 py-4 space-y-3">
            <form className="flex items-center gap-2">
              <input className="flex-1 input-soft" placeholder="Search products..." />
              <button className="btn-premium px-3 py-2 rounded-full">Go</button>
            </form>
            <div className="flex flex-col gap-2">
              <Link to="/collections/formal" className="block px-2 py-2 rounded hover:bg-gray-50">Formal</Link>
              <Link to="/collections/casual" className="block px-2 py-2 rounded hover:bg-gray-50">Casual</Link>
              <Link to="/sustainability" className="block px-2 py-2 rounded hover:bg-gray-50">Sustainability</Link>
            </div>
          </div>
        </div>
      )}
    </header>
  )
}
