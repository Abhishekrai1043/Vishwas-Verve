import React from 'react'
import './Hero.css'

export default function Hero({ title = "Visvas Verve", subtitle = "Sustainable clothes & eco-carrybags", bgImage }) {
  const inlineStyle = bgImage ? { backgroundImage: `url(${bgImage})` } : undefined

  return (
    <section className="rounded-lg overflow-hidden shadow-soft mb-6">
      <div className="relative w-full h-64 sm:h-72 md:h-80 flex items-center hero-banner" style={inlineStyle}>
        <div className="absolute inset-0 hero-overlay"></div>
        <div className="container mx-auto px-4 relative z-10">
          <div className="max-w-2xl p-6 rounded-lg bg-white/5">
            <h1 className="text-3xl sm:text-4xl font-bold text-white">{title}</h1>
            <p className="mt-2 text-sm text-gray-200">{subtitle}</p>
            <div className="mt-4 flex gap-3">
              <a href="#products" className="btn-premium">Shop now</a>
              <a href="/about" className="px-4 py-2 rounded-md bg-white/10 text-white">Learn more</a>
            </div>
          </div>
        </div>
      </div>
    </section>
  )
}
