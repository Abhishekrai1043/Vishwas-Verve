import React, {useState} from 'react'
export default function NewsletterCta(){
  const [email, setEmail] = useState('')
  function submit(e){ e.preventDefault(); setEmail(''); alert('Thanks — we will keep you posted!') }
  return (
    <section className="container mx-auto px-4 py-12">
      <div className="rounded-lg bg-gradient-to-r from-slate-900 to-slate-700 text-white p-8 flex flex-col lg:flex-row items-center gap-6">
        <div className="flex-1">
          <h4 className="text-2xl font-semibold">Stay in the loop</h4>
          <p className="mt-2 text-sm text-slate-200">Join our newsletter for early access to drops and exclusive offers.</p>
        </div>
        <form onSubmit={submit} className="flex gap-3 w-full max-w-md">
          <input required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Your email address" className="flex-1 px-4 py-2 rounded-md text-slate-800" />
          <button className="px-4 py-2 rounded-md bg-amber-400 text-slate-900 font-semibold">Subscribe</button>
        </form>
      </div>
    </section>
  )
}
